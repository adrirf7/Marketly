# MongoDB Connection
# Para desarrollo local:
MONGODB_URI=mongodb://localhost:27017/marketly

# Para MongoDB Atlas (producción):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/marketly

# JWT Secret - CAMBIAR EN PRODUCCIÓN
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-use-a-long-random-string

# Application Environment
NODE_ENV=development
